import { MergeFieldsPipe } from './merge-fields.pipe';

describe('MergeFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergeFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
